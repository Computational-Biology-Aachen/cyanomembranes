[project]
authors = [{name = "Tim Nies", email = "t.nies93@gmail.com"}]
dependencies = []
name = "cyanomembranes"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
cyanomembranes = { path = ".", editable = true }

[tool.pixi.tasks]
